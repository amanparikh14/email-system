# Generation provider (live)
OPENAI_API_KEY=

# Judge provider (live) -- must be a different model family than generation (see NFR-3)
ANTHROPIC_API_KEY=

# Optional: only needed if GEN_CHAIN includes the gemini stub
GEMINI_API_KEY=
